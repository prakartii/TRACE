// Responsible-AI view mode (ARCHITECTURE.md §15). A presentation filter only —
// there is no authentication layer, so this does not enforce access.
//
// Operator view shows the shop-floor screens; supervisor view shows everything.
export const OPERATOR_SCREENS = ['Live View', 'Action Center', 'Incidents', 'Incident Replay']
