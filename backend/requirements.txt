fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
pydantic==2.12.5
pytest==9.1.1
httpx==0.28.1
# opencv-python (not -headless): ultralytics unconditionally depends on
# full opencv-python, and installing both packages side by side corrupts
# the shared `cv2` install (they occupy the same path). Consolidated onto
# one here rather than fighting that.
opencv-python==4.13.0.92
# numpy is imported directly (backend/video/source.py, backend/perception/*),
# not just transitively. torch/ultralytics/opencv all constrain it, so keep it
# in lockstep with the rest of this file.
numpy==2.4.3
# ultralytics / supervision are intentionally newer than the original pins:
# the older releases (8.3.40 / 0.25.1) conflict on numpy>=2 / Python 3.13+,
# which is what this project's environment resolves to. These versions are
# the ones verified working locally (supervision >=0.31 would require a
# ByteTrack API migration — see backend/perception/tracker.py).
ultralytics==8.4.145
supervision==0.30.2
# Optional: the grounded assistant's phrasing layer. When absent (or no
# ANTHROPIC_API_KEY), the assistant returns the deterministic retrieval answer.
anthropic>=0.69.0
