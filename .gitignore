# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Backend runtime data
backend/db/*.db
backend/db/*.sqlite3

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Data & model artifacts populated in later phases
data/clips/*
!data/clips/.gitkeep
data/pilot_annotations/*
!data/pilot_annotations/.gitkeep
data/.perception_cache/

# Challenge-provided input videos (large binary assets, kept local only)
data/challenge_videos/*
!data/challenge_videos/.gitkeep
models/*
!models/.gitkeep

# Pilot fine-tune training run artifacts (checkpoints, logs, plots) —
# regenerable via training/train_pilot_model.py; the scripts themselves
# are tracked. Also covers Ultralytics' own default output path
# (./runs/...) for any ad-hoc `model.val()`/`model.predict()` call made
# without an explicit project= — same kind of generated artifact.
training/runs/
/runs/

# Env
.env
.env.local

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Internal AI/project instructions — keep local, never push

CLAUDE.md
README.md
data/trace.db
scratch/
